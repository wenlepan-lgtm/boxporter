# BoxPorter V1.1 实施反馈汇报（对照规划书审核）

> 报告日期：2026-08-14（含修复轮次 R1、R2、R3 更新）
> 基准文档：`搬运猴_BoxPorter_24x7无人值守AI工作台完整规划书_V1.1.md`
> 代码状态：10 个提交，169 个测试全部通过，服务已在本机部署运行
> 修复依据：`v1.1-implementation-fix-guide.md`（R1）、`v1.1-implementation-comprehensive-remediation.md`（R2）、`v1.1-ui-backend-interaction-audit.md`（R3）
> 标记说明：✅ 完成并验证　⚠️ 部分完成 / 有偏差　❌ 未做

---

## 0. 修复轮次

### R1（执行闭环，已落地）
P0-A Runner 工厂/空注册表报错、P0-B tick 观察闭环（终态自动推进）、P0-C reconcile 不伪造 handle、P1-E 审批消费接入执行点、P1-D ADR-015 恢复语义、P2-F/G 报告修正+回归测试。11 项回归测试，详见 fix-guide。

### R2（产品入口与口径统一，已落地）
Web 新建任务弹窗（粘贴 JSON / 本地文件）、客户端校验、结构化错误模型、创建成功态 + Mark READY + 跳转详情；`POST /api/tasks/import`、`GET /api/tasks/{id}/readiness`；Runner 能力矩阵；报告单一真相源。9 项回归测试。

### R3（UI/后端交互审计，已落地）
- P0-1 Run 管理 API：`GET /api/runs/{id}`（含 lease/attempt/prompt_sha）、`GET /api/runs/{id}/events`（cursor 分页）、`POST /api/runs/{id}/resume`（状态机+ADR-015 约束：仅 STALLED/WAITING_APPROVAL 可续跑；CRASHED 返回结构化拒绝并指向 retry 新 Attempt）。
- P0-2 系统健康组件化：`/api/system/health` 返回 control_plane/database/openhands/runner_registry/disk/backup/remote_access 七个组件 + warnings 列表；UI 多组件面板替代单点徽标。
- P0-3 项目域模型：顶部项目选择器；四箱看板/任务列表按 project_id 过滤；新建任务弹窗项目下拉。
- P1-4 前端统一走 `/api/tasks/import`（文件/文本共享后端解码与错误模型）。
- P1-5 SSE 游标持久化（sessionStorage）+ 跳号检测自动回放补齐 + 重连状态徽标；关键写请求全部带 Idempotency-Key（clientSessionId+action+target）。
- P1-6 任务详情结构化标签页（概览/运行/事件/提交包/审核）+ Run 独立运行页（事件轮询、停止/续跑按钮、连接状态）。
- P2-7 阻塞看板（`GET /api/blockers` + UI 列表/解阻塞）。
- 8 项回归测试（tests/integration/test_run_api.py）。

---

## 1. 分阶段路线对照（规划书 §20.3）

| Phase | 规划书完成标准 | 状态 | 验证证据 |
|---|---|---|---|
| 0 基线冻结与 ADR | 现有测试通过，关键架构决定可追溯 | ✅ | 上游 0.2.0 基线 16 测试冻结（core.py 移至 v0.py 保留）；ADR-001~014 全部落盘；README「三个箱子」→「四个箱子」修正；Runner 版本固定记录（OpenHands v1.13.0 / DeepSeek Harness commit 47f9438 / openhands-sdk 1.42.1）；迁移策略 PRAGMA user_version 前向追加 |
| 1 协议内核 V2 | 无 Agent 也可完整模拟状态流转；无变化 tick 零模型调用 | ✅ | Task/Attempt/Run/Event 四类 Schema；6 个 SQLite migration；强类型状态机（合法迁移表强制）；21+ 个幂等命令（operations 表 operation_id 重放）；四箱投影；测试：完整流转模拟 + 幂等重放 + CAS 乐观锁 + 事件游标 |
| 2 Lease、WatchDog 与 Command Runner | 进程崩溃、宿主重启、心跳过期和旧进程写入均有自动测试 | ✅ | Lease 表（task+role 唯一约束）；fencing token 全局单调递增、旧 token 心跳/释放被拒；WatchDog 四类发现（LEASE_MISSING / LEASE_EXPIRED / STALE_HEARTBEAT / PROCESS_DEAD / NO_PROGRESS）；恢复引擎任务级累计预算；reconcile 宿主重启恢复；Command Runner 真进程退出码测试 |
| 3 Git 提交与独立审核 | 提交后任意代码或证据变化都会使原审核失效 | ✅ | Git Worktree 生命周期管理；Submission Manifest V2（canonical JSON + submission_sha256 绑定 commit/tree/diff/报告哈希）；执行/审核身份+Session+Worktree 强制隔离；Acceptance Gate 顺序检查；PASSED 证据包封箱（Secret 扫描阻断）+ 离线 verify-package；篡改代码/证据后审核被拒有测试锁定 |
| 4 OpenHands 主 Runner Adapter | 两个隔离 Session 完成执行→审核→PASS/REVISE 端到端；浏览器断开不影响执行 | ✅⚠️ | Adapter（openhands-sdk 1.42.1 固定、惰性导入、Bridge 可注入）+ 契约测试 9 项（FakeBridge）+ 2 项 env 门控真实 SDK 测试 ✅；浏览器断开不影响执行（daemon 线程模型）✅；**Daemon 重启后重新关联存活 OpenHands Conversation ❌ 未完成**（ADR-015：SDK 无恢复语义，重启后诚实判 CRASHED 走预算内新 Attempt，不伪造存活） |
| 5 远程 Web 控制台 | 外部网络登录后无需读数据库即可判断真实状态并处理阻塞 | ✅⚠️ | 登录（scrypt+HMAC Cookie）、设备会话/撤销/重认证（高风险 401+reauth）、CSRF 中间件、远程操作成功+失败 100% 审计、四箱 Dashboard、任务详情（Attempt/Run/提交/审核/事件）、阻塞与审批中心、模型/Prompt/策略设置、SSE 事件流带游标+Last-Event-ID 断线补发（零缺口测试）、任意时间窗报告 ✅；**HTTPS/身份网关/Tailscale 实际接入 ❌**（当前 127.0.0.1 监听 + SSH 隧道可用） |
| 6 24×7 Away Mode 与周期报告 | 连续 24 小时无人值守模拟不重复启动、不超预算、不绕过审批 | ✅⚠️ | SUPERVISED/AWAY/PAUSED 三策略（Web 改即时生效）；风险准入门；任务 Token 预算 + 每日 Token 上限（超额安全停止+通知）；BLOCKED 条件探针零模型解阻塞；通知去重；错误指纹三连停 + 指数退避抖动；24h + 72h 加速长稳模拟 ✅；**真实墙钟 24–72h 长稳 ❌**（需实机执行） |
| 7 Mac mini 生产化 | 重启后自动恢复，状态和证据无损，远程入口不暴露 Web | ✅⚠️ | launchd daemon+web 已实际加载运行并验证重启拉起 ✅；在线备份（SQLite backup API + 哈希清单）+ 恢复演练脚本 + 演练校验 ✅；newsyslog 轮转配置、一键安装脚本、安全清单 ✅；**UPS/断电自动开机、FileVault、newsyslog 安装到 /etc、备份定时任务 ❌**（脚本已备，需实机执行） |

---

## 2. 附录 B 开发任务清单对照（BP-001 ~ BP-028）

| 状态 | 任务 |
|---|---|
| ✅ 全部完成（26 项） | BP-001 基线冻结、BP-002 文档修正、BP-003 Schema、BP-004 SQLite+Event、BP-005 状态机幂等、BP-006 Lease、BP-007 Command Runner、BP-008 有效进展观察、BP-009 故障分类有限恢复、BP-010 Worktree、BP-011 Submission V2、BP-012 Artifact Manifest+脱敏（基础版）、BP-013 Reviewer 隔离、BP-014 Acceptance Gate+离线校验、BP-015 OpenHands 固定、BP-016 OpenHands Adapter、BP-017 DSH Adapter、BP-018 契约测试、BP-019 FastAPI、BP-020 认证审计、BP-021 事件游标、BP-022 Dashboard 四箱、BP-023 详情页、BP-024 Blocker+Approval、BP-025 Away/预算/报告 |
| ⚠️ 需实机（3 项） | BP-026 私人网络/身份网关实际配置、BP-027 newsyslog 安装与备份定时、BP-028 实机 24–72h 长稳与故障注入 |

---

## 3. 核心验收场景对照（规划书 §21.1，25 项）

**✅ 已满足（23 项）**：

1. 无变化 tick 零模型 ✅ 2. 单 Executor Session ✅ 3. Lease 持续更新不重复启动 ✅ 4. fencing 旧 Run 无法提交 ✅ 5. 执行/审核 Run+Session+身份+Worktree 全不同 ✅ 6. Reviewer 不继承执行者上下文 ✅ 7. 不能自审 ✅ 8. 审核后修改自动失效 ✅ 9. REVISE 新 Attempt/新提交/新 Session ✅ 10. PASS 证据离线重算 ✅ 11. BLOCKED 零模型重试 ✅ 12. 瞬时失败退避不重复副作用 ✅ 13. 指纹阈值停止原样重试 ✅ 14. 预算耗尽安全停止 ✅ 16. Session 不入证据包 ✅ 17. Secret 扫描阻断封箱 ✅ 18. 宿主重启恢复 ✅ 20. 升级 Runner 不动 Core ✅ 21. 浏览器断开 Run 继续 ✅ 22. SSE 游标补发 ✅ 23. 未授权拒绝 ✅ 24. 高风险重认证+审计 ✅ 25. Daemon 重启安全恢复（Command Runner 按 pid 重挂）

**⚠️ 部分满足（2 项）**：

- **#15 Away 禁止动作**：风险准入门 + 默认禁止清单 + 审批记录已实现；但**审批消耗检查尚未接入实际命令执行点**（Runner 执行前不查 approval），属下一轮接入点。
- **#19 SQLite 与投影不一致修复**：四箱为纯函数投影、无缓存副本，天然一致；未实现对照修复流程（按设计不需要）。

---

## 4. 与规划书的关键偏差（需审核确认）

| # | 偏差 | 原因 |
|---|---|---|
| 1 | tasks 表未存 `box` 列，四箱由 state 投影 | ADR-003 决策，避免双写真相；已记录 |
| 2 | `CompleteExecutorRun` 改为 `SubmitExecutorRun`：必须冻结提交才能 REVIEW_PENDING | 落实「未冻结提交禁止进入 REVIEW_PENDING」禁止迁移，比原设计更严格 |
| 3 | Checkpoint 自动流程未实现 | 接口（checkpoint/CheckpointRef）已定义；ADR-015 明确仅 supports_resume 的 Runner 参与恢复编排，OpenHands/Command 走新 Run/新 Attempt |
| 4 | 预算 80% 预警未做 | 100% 停止+通知已做；预警可后续加 |
| 5 | 通知渠道仅 log 表 | 无邮件/webhook 渠道；表结构支持扩展 |
| 6 | 旧 `.boxporter` 目录 importer 未做 | v0.py 保留可独立运行；V1 无旧数据迁移工具 |
| 7 | OpenHands 版本记录为 v1.13.0（Agent Server）+ SDK 1.42.1（适配器实际依赖） | SDK 与 Server 是两条版本线，均固定 |
| 8 | （R1 已修复）调度器曾无观察闭环、approval 曾未接执行点 | 修复轮次 R1 见 §0 |

---

## 5. §22.2 用户拍板项（当前采用的默认值）

| 事项 | 当前默认 | 待定 |
|---|---|---|
| 远程入口 | 127.0.0.1 + SSH 隧道（`ssh -L 3088:127.0.0.1:3088 -p 21008 Alamn@rmt.joctv.cn`） | Tailscale 还是身份网关 |
| 备用 Runner 优先级 | DSH Adapter 已做；Codex/Claude/GLM 未做 | 是否补做 |
| Token/费用预算 | 任务 20 万、日 200 万 token | 按实际费率调整 |
| Away 允许最高风险 | low + medium | 是否放开 |
| Planner 是否入 V1 | 未入（prompt 模板已备） | 是否纳入 |
| 通知渠道 | log 表 | 邮件/webhook |
| Artifact/Session 保留期 | 未设 | 需定 |

---

## 6. 部署现状

- launchd：`com.boxporter.daemon` + `com.boxporter.web` 已加载运行（KeepAlive 自动重启）
- Web 控制台：`http://127.0.0.1:3088`（仅本机监听），账号 `admin`
- 数据库：`data/boxporter.sqlite`（6 个 migration），项目 `boxporter`
- 密码：`data/.admin-password`（600 权限，不入 Git）

---

## 7. 如何审核验证

```bash
cd /Users/alamn/BoxPorter
uv run pytest -q                              # 139 全绿
uv run mypy && uv run ruff check src tests    # strict 全绿
uv run --extra openhands pytest tests/contract/test_openhands_sdk_real.py  # 2 passed
git log --oneline                             # 5 个提交
curl http://127.0.0.1:3088/api/system/health  # 服务在线
launchctl list | grep com.boxporter           # daemon + web 运行中
```

---

## 8. 剩余实机待办（按优先级）

1. **配置真实 Runner**：OpenHands Agent Server（`uv sync --extra openhands`）+ 模型凭据入 Keychain，完成首次真实执行→审核→封箱闭环
2. **远程入口**：安装 Tailscale 或确认 SSH 隧道方案，外部网络实测登录
3. **宿主可靠性**：UPS + 断电自动开机（系统设置）、FileVault、`sudo cp operations/newsyslog/com.boxporter.conf /etc/newsyslog.d/`
4. **备份制度化**：备份脚本接入每日定时，`restore-drill.sh` 每月演练
5. **实机长稳**：24–72 小时无人值守 + 断网重连 + 重启故障注入
6. **安全清单**：按 `docs/operations/security-checklist.md` 逐项确认
7. **审批执行点接入**：Runner 执行高风险动作前消费 approval（验收 #15 补全）

---

## 9. 结论（R1/R2/R3 修正后）

三轮修复均基于外部评审的真实缺口判断：R1 闭环执行路径（空注册表、观察闭环、伪 handle、审批接入），R2 闭环产品入口（Web 任务创建、结构化错误、启动条件、能力边界），R3 闭环 UI/后端交互契约（Run 管理 API、组件化健康、项目域过滤、import 统一、SSE 游标补齐、详情标签页与运行页、阻塞看板）。当前状态：**169 项测试全绿，`project → new task(json/file) → ready → running → run page → stop/resume → final` 全链路可操作**。剩余待办仍属实机环境、凭据与外部服务范畴：真实 Runner 接入（OpenHands/模型 Key）、远程入口（Tailscale/隧道）、UPS/备份制度化、实机 24–72h 长稳、安全清单确认（见 handoff/STATUS.md 待办）。
